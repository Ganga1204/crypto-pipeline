# k8s/service.yaml
# This creates a LoadBalancer so we can access the Streamlit dashboard

apiVersion: v1
kind: Service
metadata:
  name: crypto-pipeline-service
spec:
  selector:
    app: crypto-pipeline
  ports:
  - protocol: TCP
    port: 8501
    targetPort: 8501
  type: LoadBalancer
